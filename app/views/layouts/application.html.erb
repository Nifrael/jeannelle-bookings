<!DOCTYPE html>
<html>
  <head>
    <title>JeannelleBookings</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if !therapist_signed_in? %>
      <%= link_to "Se connecter", new_therapist_session_path %>
    <% else %>
      <%= link_to 'Se déconnecter', destroy_therapist_session_path, data: { turbo_method: :delete, turbo_confirm: 'Êtes-vous sûr ?' } %>
    <% end %>
    <%= yield %>
  </body>
</html>
