<header>
  <nav>
    <ul>
      <li>
        <%= link_to 'Accueil', root_path %>
      </li>
      <li>
        <%= image_tag 'Jeannelle.png' %>
      </li>
      <li>
        <% if !therapist_signed_in? %>
          <%= link_to "Espace Pro", new_therapist_session_path %>
        <% else %>
          <% if current_therapist.admin? %>
            <%= link_to 'Tableau de bord', admin_dashboards_path %>
          <% else %>
            <%= link_to 'Tableau de bord', therapist_dashboards_path %>
          <% end %>
        <% end %>
      </li>
    </ul>
  </nav>
  <div class="gradient">
  </div>
</header>
